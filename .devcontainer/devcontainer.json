{
  "name": "Docker Env Parser",
  "dockerComposeFile": "docker-compose.yml",
  // VSCodeで接続するコンテナ
  "service": "parser",
  // VSCodeで開くフォルダ
  "workspaceFolder": "/app",
  // 拡張機能
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "ms-python.debugpy", "ms-azuretools.vscode-docker", "streetsidesoftware.code-spell-checker"]
    },
    "settings": {
      "editor.formatOnSave": true
    }
  }
}
